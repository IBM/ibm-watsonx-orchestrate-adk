# ============================================================================
# IBM watsonx Orchestrate ADK - Environment Configuration Example
# ============================================================================
# Copy this file to .env and update with your configuration values
# 
# Documentation: https://developer.watson-orchestrate.ibm.com
# ============================================================================

# ============================================================================
# RETRY CONFIGURATION
# ============================================================================
# Configure retry behavior for transient errors and timeouts
# These settings apply globally to all API operations unless overridden

# Maximum number of retry attempts for failed requests
# - Default: 3
# - Recommended: 3-5 for production, 1-2 for development
# - Set to 0 to disable retries completely
ADK_MAX_RETRIES=3

# Initial retry interval in milliseconds
# - Default: 1000 (1 second)
# - This is the base wait time before the first retry
# - Subsequent retries use exponential backoff (doubles each time)
# - Example progression with default: 1s → 2s → 4s → 8s
ADK_RETRY_INTERVAL=1000

# Multiplier for exponential backoff between retries
# - Default: 2.0 (doubles the wait time)
# - Range: 1.5-3.0 recommended
# - Lower values = more aggressive retries
# - Higher values = more conservative retries
ADK_BACKOFF_MULTIPLIER=2.0

# Jitter percentage to add randomness to retry timing
# - Default: 0.2 (±20% variation)
# - Range: 0.0-0.5 recommended
# - Prevents thundering herd problem when multiple clients retry
# - 0.0 = no jitter, 0.5 = ±50% variation
ADK_JITTER_PERCENTAGE=0.2

# Request timeout in seconds
# - Default: 300 (5 minutes)
# - Recommended values by operation type:
#   * Fast API calls: 30-60 seconds
#   * Document processing: 300-600 seconds
#   * ML model inference: 120-300 seconds
#   * Data exports: 600+ seconds
ADK_TIMEOUT=300

# ============================================================================
# RECOMMENDED CONFIGURATIONS BY ENVIRONMENT
# ============================================================================

# Development/Testing Environment:
# ADK_MAX_RETRIES=1
# ADK_RETRY_INTERVAL=500
# ADK_TIMEOUT=30

# Staging Environment:
# ADK_MAX_RETRIES=3
# ADK_RETRY_INTERVAL=1000
# ADK_TIMEOUT=120

# Production Environment (Default):
# ADK_MAX_RETRIES=3
# ADK_RETRY_INTERVAL=1000
# ADK_TIMEOUT=300

# High-Reliability Production (Critical Operations):
# ADK_MAX_RETRIES=5
# ADK_RETRY_INTERVAL=2000
# ADK_TIMEOUT=600

# ============================================================================
# AUTHENTICATION CONFIGURATION
# ============================================================================
# Add your authentication credentials here

# IBM watsonx API Key
# WATSONX_APIKEY=your_api_key_here

# IBM watsonx URL
# WATSONX_URL=https://us-south.ml.cloud.ibm.com

# IBM watsonx Space ID
# WATSONX_SPACE_ID=your_space_id_here

# ============================================================================
# OTHER CONFIGURATION OPTIONS
# ============================================================================
# Add other environment-specific configurations as needed

# Example: Enable debug logging
# LOG_LEVEL=DEBUG

# Example: API base URL for local development
# WXO_BASE_URL=http://localhost:4321

# Example: Disable SSL verification for local development (not recommended for production)
# SSL_VERIFY=fal