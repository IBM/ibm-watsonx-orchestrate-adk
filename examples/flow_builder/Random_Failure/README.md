1. Run `import-all.sh` 
2. Launch the Chat UI with `orchestrate chat start`
3. Pick the `testAgent agent`
4. Type in something like `random failure`

### Flow Information
In this example, there are two tools. The input value should be between 0 and 1. The closer the value is to 1, the higher the probability that the first tool will fail.